{
  "name": "web-permission-messages",
  "version": "1.0.1",
  "description": "Messages and steps when permissions on web are denied",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && npm run screenshots",
    "screenshots": "copyfiles -f src/screenshots/* dist/screenshots"
  },
  "author": "Shreyas Jadhav",
  "license": "ISC",
  "dependencies": {
    "platform": "^1.3.6"
  },
  "devDependencies": {
    "@types/platform": "^1.3.4",
    "copyfiles": "^2.4.1"
  }
}
